import * as React from "react";
import { Button } from "../../../src/components/Button";

const Component = () => {
  return (
    <Button onClick={() => {}} color="primary">
      Button text
    </Button>
  );
};
