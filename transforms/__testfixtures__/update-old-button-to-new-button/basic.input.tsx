import * as React from "react";
import { Button } from "../../../src/components/Button";

const Component = () => {
  return (
    <Button onClick={() => {}} color="primary">
      Old primary button
    </Button>
  );
};
